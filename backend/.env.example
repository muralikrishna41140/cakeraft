# Environment Configuration
NODE_ENV=development
PORT=5000

# MongoDB Configuration
MONGODB_URI=mongodb+srv://p1:p1@cluster0.iaorkfe.mongodb.net/billing-system

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=7d

# Admin Credentials (for initial setup)
ADMIN_EMAIL=admin@billing.com
ADMIN_PASSWORD=Admin@123

# Upload Configuration
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,image/webp

# Cloudinary Configuration (for image storage)
# Sign up for free at https://cloudinary.com/users/register_free
# Get your credentials from Cloudinary Dashboard
CLOUDINARY_CLOUD_NAME=dqogdph2l
CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# WhatsApp Business Cloud API Configuration
# Get these credentials from your Meta Developer Dashboard
# See WHATSAPP_SETUP.md for detailed setup instructions
WHATSAPP_API_TOKEN=your_whatsapp_business_api_token_here
WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id_here

# Optional: Custom WhatsApp API Base URL (default: https://graph.facebook.com/v17.0)
# WHATSAPP_API_BASE_URL=https://graph.facebook.com/v17.0

# Google Sheets Configuration (for revenue data export)
# 1. Create a Google Cloud project and enable Google Sheets API
# 2. Create service account and download credentials JSON
# 3. Save credentials as: backend/src/config/google-credentials.json
# 4. Create a Google Sheet and copy its ID from the URL
# 5. Share the Google Sheet with the service account email (as Editor)
GOOGLE_SHEETS_SPREADSHEET_ID=your_google_spreadsheet_id_here
GOOGLE_APPLICATION_CREDENTIALS=./src/config/google-credentials.json

# Loyalty Program Configuration
LOYALTY_FREQUENCY=3
LOYALTY_DISCOUNT_PERCENTAGE=10